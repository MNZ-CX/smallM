# 更新日志

## v1.0.1

修掉最小窗口下展开月历「文字叠在一起」的问题，并把**提醒时间改成点选，不用再手打**。

### 🐞 修复

- **最小窗口下展开月历，日期文字挤在一起 / 与日程面板重叠**
  月历改为「按需长高」：窗口装不下时自动增高并同步抬高最小高度，展开期间用户也无法把窗口拖到重叠状态；收起后窗口高度与最小高度都会还原。
- 窗口标题重复显示应用名（`悬浮日历备忘 … - 悬浮日历备忘`）已修正。

### ✨ 新增

- **右键输入框（或点左侧 ⏰ 胶囊）直接选提醒时间**，常用时间一点即选：
  不提醒 · 立即 · 10 分钟后 · 30 分钟后 · 1 小时后 · 今天 09:00 / 12:00 / 15:00 / 18:00 / 21:00 · 明天 09:00 · **自定义…**（时间选择器，精确到分钟）
  选完后 ⏰ 胶囊变蓝显示时间，回车即入库，提交后自动复位。
- 右键菜单同时保留粘贴 / 复制 / 剪切。
- 手打时间依然有效：`15:00 部门例会`、`明天 10:00 评审` 等写法照旧，**文字里的时间优先**于面板选择。
- 首次运行就在同级目录生成 `memos.json`（方便随时备份，也能立刻发现目录不可写）。
- 窗口标题与托盘提示都带版本号，便于确认用的是哪一版。

### 🔄 升级

用新的 `smallM.exe` **覆盖**旧文件即可；`memos.json` 不会被动，画布内容、日程、设置全部保留。
（覆盖前请右键托盘图标 →「退出程序」，只点窗口 `✕` 只是退到托盘，进程仍在运行。）

---

## v1.0.0

首个版本：极简暗黑悬浮备忘 / 日历 / 日程工具。

- **5 张独立画布**：顶部莫兰迪彩色圆点切换，150ms 淡入，打字即时落盘
- **动态月历**：有记录的日期带发光热度点，鼠标悬停浮出当天全部待办
- **日程 + 到点提醒**：全屏居中覆盖提醒（知道了 / 延后 10 分钟 / 标记完成）+ Windows 系统提示音
- **历史回溯**：点月历任意日期，下方清单原地切换为该日日程
- **多条目清单**：逐条独立卡片（复选框 + 时间胶囊 + 🗑），勾选自动划线沉底
- **零阻尼交互**：全程无确认弹窗；删除/清空秒删 + 底部滑出 3 秒撤销条
- **一键导出**：当天待办整理成 `1. 2. 3.` 编号列表进剪贴板，直接粘贴
- **可自由调节**：拖边缘或右下角抓手缩放，尺寸记忆；画布高度随内容自适应
- **Raycast 暗黑风**：`#0d0e11` 底 + `#16181d` 卡片 + 1px 微发光边框 + 14px 圆角 + 20px 弥散阴影
- **防打扰悬浮**：鼠标移出透明度降到 `0.35`，移入 0.15s 恢复，🔒 可锁定常显
- **全局速记**：任何软件里按 `Alt+Q` 浮出输入条，回车即入库（`@3` 指定写入第 3 张画布）
- **纯本地**：数据只写入同级目录的 `memos.json`，不联网、不上传、无遥测
